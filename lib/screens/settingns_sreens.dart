import 'package:flutter/material.dart';

class SettignsScreens extends StatelessWidget {
  const SettignsScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Tela de configurações'),
        centerTitle: true,
      ),
    );
  }
}
